<?php
    //commento a riga singola

    /*
    commento 
    a
    più
    righe
    */

    $x = 5; //<-- inizializzo la variabile X il tipo di dato viene acquisito dal contenuto

    // variabile si inizializza con il segno di dollaro ($)

    if($x % 2 == 0) echo "Il numero $x &egrave; divisibile per 2";
    else        echo "Il numero $x non &egrave; divisibile per 2";

    // echo = Dare in output
    // Nella stampa "$x" prende il valore della variabile "x" e lo stampa:
    // Stamperebbe: Il numero 5 non è divisibile per 2

    // &egrave = si chiama "entità": prende il simbolo "é" e lo stampa in HTML
    // Esempio pratico --> segno di copyright (C) in HTML --> &copy
?>

